{
  "name": "azure-sas-token-web",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Client-only web UI to generate Azure SAS tokens in the browser.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc -p . --noEmit"
  },
  "dependencies": {
    "solid-js": "^1.9.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^5.4.0",
    "vite-plugin-solid": "^2.11.0"
  }
}
